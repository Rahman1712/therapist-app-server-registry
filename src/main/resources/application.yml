eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    
server:
  port: 8761
  
cors:
  set:
    allowed:
      origins: ${ALLOWED_ORIGINS:http://localhost:5173/,http://localhost:3000/,http://localhost:4200/}
